
    <header>
        <div class='row col-md-12'>
            <div class='col-md-4 '>
                <h1>Veteres</h1>
                <p>Thrift Store Goods</p>
            </div>
            <div class="col-md-4"></div>
            <div class='col-md-4 '>
                <div class="login grid-x tc-right">
                    <h6>
                        <a href="#" id="loginLink" data-toggle="exampleModal1" >{{merch.0.usertbl.username}}</a>
                    </h6>
                </div>
            </div>
        </div>
        </header>
<div class='container tc-spacer'></div>
<div class='container '>
  <div class='row'>
    {{#each merch}}
    <div class="card " style="width: 20rem;">
      <img height='180px' class="card-img-top" src="{{photolink}}" alt="Card image cap">
      <div class="card-body">
        <h4 class="card-title" style="text-align: top;">{{title}}</h4>
        <h6 class="card-text ">{{description}}</h6>
        <a href="#" class="btn btn-primary">Update item</a>
        <a href="#" class="btn btn-primary">Delete item</a>
      </div>
    </div>
    {{/each}}
  </div>
</div>

